# Xcode
*.xcodeproj/
xcuserdata/
*.xcworkspace/
DerivedData/
build/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
Packages/

# Rust (DeepFilterNet bridge) — build output only.
# Cargo.lock IS committed: it pins every transitive crate so the XCFramework is
# reproducible. Vendor/DeepFilterBridge.xcframework is also committed so app
# builds need no Rust toolchain.
native/deepfilter-bridge/target/
native/deepfilter-bridge/.cargo/

# macOS
.DS_Store
*.swp
*~

# Misc
*.log
